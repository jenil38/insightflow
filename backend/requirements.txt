# Pinned to the version the test suite actually runs against. Starlette is
# pinned explicitly too: FastAPI's own range is wide, and Starlette 1.x renamed
# status constants, so leaving it floating means a fresh install can behave
# differently from a verified one.
fastapi==0.135.3
starlette==1.6.0
uvicorn[standard]==0.32.0
sqlalchemy==2.0.35
python-jose[cryptography]==3.3.0
passlib==1.7.4
bcrypt==4.0.1
python-multipart==0.0.12
pydantic[email]==2.9.2
pydantic-settings==2.5.2
python-dotenv==1.0.1
psycopg2-binary==2.9.9
pandas==2.2.2
openpyxl==3.1.5
scikit-learn==1.5.2
shap==0.46.0
requests==2.32.3
reportlab==4.2.5

# --- Phase 1 additions ---
slowapi==0.1.9          # rate limiting
alembic==1.13.2         # database migrations

# --- Phase 4 additions (ML engine upgrade) ---
xgboost==3.0.5
lightgbm==4.7.0
catboost==1.2.10
joblib==1.4.2

# --- Testing ---
pytest==8.3.3
pytest-asyncio==0.24.0
httpx==0.27.2
